# POD-Bench requirements
# Install: pip install -r requirements.txt

# ── Core ────────────────────────────────────────────────────────────────
requests>=2.28
numpy>=1.24
Pillow>=9.0
tqdm>=4.64

# ── OpenAI-compatible API (gpt-4.1-nano, gpt-4.1-mini, vLLM 등) ────────
openai>=1.0

# ── Tokenizer (비용 추정용, tiktoken) ───────────────────────────────────
tiktoken>=0.7

# ── Image collection pipeline (optional) ────────────────────────────────
# text_only_eval 만 쓴다면 아래 두 줄은 불필요
# open_clip_torch>=2.20
# scikit-image>=0.20

# ── CLIP retrieval backend (optional, collect_images_clip_retrieval.py) ─
# pip install clip-retrieval  (별도 설치)
